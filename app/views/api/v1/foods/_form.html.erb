<%= form_with(model: api_v1_food) do |form| %>
  <% if api_v1_food.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(api_v1_food.errors.count, "error") %> prohibited this api_v1_food from being saved:</h2>

      <ul>
        <% api_v1_food.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
